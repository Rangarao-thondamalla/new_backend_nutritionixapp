server:
  port: 8888

spring:
  cloud:
    config:
      server:
        native:
          search-locations: file:///G:/nutritionix-app-ui-capstone-project/config-repo
  profiles:
    active: native
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost